{
  "name": "jbee-trip-editor-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc --noEmit false --outDir dist",
    "types": "wrangler types",
    "deploy:dry": "wrangler deploy --dry-run",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.33.1"
  }
}
